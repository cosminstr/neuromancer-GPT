This sub-dir contains source code for distilling gpt-oss-20b inside neuromancer-gpt. The goal is to get a good enough transformer model using  only the sprawl corpus, without requiring additional text.

To run this you will have to add HF_TOKEN and WANDB_API_KEY to a distillation/.env file.
